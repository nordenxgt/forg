[build-system]
requires = ["setuptools>=84.0.0", "wheel>=0.48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "forg"
version = "0.1.0"
description = "File organizer CLI tool"
readme = "README.md"
authors = [{name = "nordenxgt"}]
requires-python = ">=3.10"

[project.scripts]
forg = "forg.cli:main"
